name: CI/CD - Testes Unitários e Qualidade

on:
  push:
    branches:
      - main
      - dev
      - hml
      - feat/implementa-testes-estacao
  pull_request:
    branches:
      - main
      - dev
      - hml
      - feat/implementa-testes-estacao

jobs:
  test_unitario:
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 Checkout do Código
        uses: actions/checkout@v4

      - name: 🛠️ Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm' 

      - name: 📦 Instalar Dependências
        run: npm install
        
      - name: 🧪 Executar Testes Unitários (Vitest)
        run: npm test
        env:
          NODE_ENV: test 

      - name: 📤 Upload de Artefato de Cobertura
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: relatorio-cobertura
          path: coverage/lcov.info

          retention-days: 7
